// // String without type annotation
const firstName = "Mark";
console.log(`firstName: ${firstName}`);

// String with type annotation
const lastName: string = "Wetzel";
console.log(`lastName: ${lastName}`);
